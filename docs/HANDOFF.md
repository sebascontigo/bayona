# HANDOFF — BAYONA WEB

> Corto y operativo. Permite a otro agente continuar SIN leer conversaciones. Orden de lectura completo: PROJECT-STATE → HANDOFF → ROADMAP → DECISIONS → docs de fase → Git.

## ¿Qué acaba de ocurrir? (2026-08-30)
1. Auditoría forense de Fases 1–5 sobre `70606dd`: veredicto **✅ C APROBADA** (29/30 CONFIRMADO, 1 PARCIALMENTE CONFIRMADO, 0 CONTRADICHO). Gates ejecutados de verdad: 381/381 tests, lint 0/16, build OK, 41/41 Playwright. Producción = build de HEAD.
2. Primera sincronización documental V1.0: creado `docs/` con el sistema central (commit `2de0a6c`); documentación de fase intacta en raíz.
3. Sincronización final post-auditoría independiente de ChatGPT: referencias actualizadas (commit `bfcf6b4`, AUDIT-LOG entrada 004). ChatGPT: "🟢 B — APROBADA PARA AVANZAR".
4. **DF-006 APROBADA** (confirmación explícita de Sebastián vía veredicto ChatGPT + entrega del PROMPT MAESTRO FASE 6): FASE 6 = WORLD BUILDING. Pre-vuelo de Fase 6 ejecutado (HEAD = origin/main, árbol limpio); PLAN OPERATIVO FINAL presentado y subido al repo como `PLAN-FASE-6-WORLD-BUILDING.md` (raíz), esperando aprobación (AUDIT-LOG entradas 005 y 006).
5. **Auditoría del plan (ChatGPT sobre `046a37c`): "🟢 APROBADO PARA AVANZAR, con correcciones obligatorias"** → registradas como D-008. Sebastián entregó el PROMPT MAESTRO V2.0 (nuevo prompt rector). El agente ejecutó de verdad el checkpoint de verificación (PASOS 1–3 de V2.0): HEAD = origin/main = `046a37c` confirmado; gates re-ejecutados (381/381 · 0 errores/16 warnings · build OK vendor-three 826.94 kB · 41/41 E2E); inventario de rutas normalizado por categorías (16 canónicas + 1 alias + 404 + 1 interna) documentado en el plan §0.2; las 4 correcciones obligatorias aplicadas al plan en el repo (AUDIT-LOG entrada 007). Commit `a808e5e`.
6. **BLOQUE 1 EJECUTADO (2026-08-28)** — autorización del auditor: "🟢 AUTORIZADO: INICIAR ÚNICAMENTE EL BLOQUE 1 DE FASE 6". Pre-flight real (HEAD = origin/main = `a808e5e`, árbol limpio); relectura completa de fuentes; verificación crítica de los 10 principios (sin problema material: se mantienen los 10, análisis de solapamientos documentado en el documento §D.11); creado `BAYONA-WORLD-BIBLE.md` PARTE I (ADN VISUAL) en la raíz: qué es/no es BAYONA, 10 principios operativos con 10 campos cada uno, curva emocional derivada de J1–J10, materialidad (literal/sugerida/prohibida), luz (6 tipos sobre tokens existentes), profundidad (4 tipos, 3D como excepción), densidad/ritmo compatibles con el engine, autoauditoría K.1–K.4. Gates re-ejecutados de verdad: 381/381 (35.95 s) · 0/16 · build 16.11 s · 41 visual (4.0 min). 0 archivos de producción tocados (AUDIT-LOG entrada 008). Commit `3a9f511`.
7. **BLOQUE 1 APROBADO por el auditor** (veredicto sobre `3a9f511`, 2026-08-28) con 4 condiciones vinculantes registradas: **DF-009** disciplina de evidencia, **DF-010** territorio no es folclore, **DF-011** diferenciación real de mundos (gobernará el Bloque 3), **DF-012** anti-burocracia. **BLOQUE 2 EJECUTADO (2026-08-28)**: creado `SPATIAL-LANGUAGE.md` (gramática espacial) en la raíz — 7 estados espaciales (entrada/aproximación/tensión/inmersión/descubrimiento/decisión/salida) con qué produce cada uno, qué principios expresa y gramática conceptual de movimiento ("puede expresarse mediante", nunca APIs); transiciones permitidas/prohibidas; 5 clases de página con estados estructurales/admitidos/prohibidos; mobile; reduced motion; regla absoluta de claridad; autoauditoría 6 tests. Gates re-ejecutados de verdad: 381/381 (37.93 s) · 0/16 · build 13.49 s · 41 visual (3.7 min). 0 producción tocada (AUDIT-LOG entrada 009).
8. **BLOQUE 2 APROBADO por el auditor** (autorización del BLOQUE 3 entregada por Sebastián con dos prompts: autorización + prompt complementario maestro). **BLOQUE 3 EJECUTADO (2026-08-28)**: `BAYONA-WORLD-BIBLE.md` PARTE II — MUNDOS 00–08 (~950 líneas añadidas). Pre-flight real (HEAD = origin/main = `d8d6009`, árbol limpio); relectura completa y real de todas las fuentes obligatorias (docs de fase, identidad, config, engine verificado contra código). Resultado crítico: **9 mundos mantenidos, 0 fusiones, 2 refinamientos de definición documentados** (CUERPO: el cuerpo como realidad/biografía frente a cliché estético; EXPERIENCIA: ecosistema sin membresía). Cada mundo con pregunta central, test de existencia A–D, función y psicología, espacio/composición, materia/luz/densidad, movimiento (vocabulario exacto del engine), profundidad/3D, medios, uso/antiuso, herencia y variación, rutas núcleo/periferia, resiliencia, implicación Fase 7/8, anti-genericidad, elemento de identificación. Matrices: diferenciación (9 filas + análisis crítico), medios, resiliencia; prueba de intercambiabilidad (7 pares sensibles + 36 combinaciones pairwise, todas SEPARADOS); mapa de relaciones y análisis del orden 00–08; autoauditoría completa; hallazgos F6-01…F6-05. 3D: 6 PROHIBIDO / 1 OPCIONAL (ORIGEN) / 1 JUSTIFICADO (MOVIMIENTO) / 1 EXCEPCIONAL (CUERPO, Globe3D vivo) — sin objetos, modelos ni escenas. Gates re-ejecutados de verdad: 381/381 (33.79 s) · 0/16 · build 16.41 s · 41 visual (5.2 min). 0 producción tocada (AUDIT-LOG entrada 010).
9. **BLOQUE 4 EJECUTADO (2026-08-28)** — autorización: PROMPT MAESTRO de Sebastián (pre-flight real, relectura completa, cobertura 16 canónicas + alias + fallback + nota interna, 15 apartados por blueprint, auditoría de no-intercambiabilidad, protecciones de embudo/claridad/planes, 3D solo clasificar, gates reales, commit atómico + push, informe A–S, STOP absoluto). Pre-flight real (HEAD = origin/main = `252943c`, árbol limpio, `PAGE-BLUEPRINTS.md` inexistente); relectura de los 13 documentos + fuentes comerciales (`offerings.js`, `conversionContent.js`, `shopCatalog.js`, `testimonials.js`, `chapters.js`, `routeMeta.js`) + verificación contra código vivo (8 recetas, 3 intensidades, 5 zonas, 4 rangos, `resolvePageMotionContract()`, Globe3D como excepción real). Entregable: `PAGE-BLUEPRINTS.md` (1210 líneas) — COBERTURA demostrada matemáticamente; 18 blueprints (B01–B18) con los 15 apartados obligatorios cada uno; momentos irreductibles sin repetición; protección del embudo (`/checkout`), de la continuidad (`/order-confirmation`), del umbral (`/onboarding`), de `/about` (Globe3D excepcional, no licencia), de `/plan/*` (sistema compartido sin 4 copias; DP-5 intacto), de `/faq` y del 404 (recoloca, no vende); auditoría de no-intercambiabilidad TEST 1–4 con 5 parejas de riesgo explícitas; MAPA DE RELACIONES + tabla §8.2 (semilla del BLOQUE 5); 3D solo clasificado (0 escenas). Autoauditoría verificada con grep/diff (no declarada): 73 menciones de las 8 recetas reales, 0 inventadas; intensidades solo quiet/balanced/immersive; 0 APIs ni contratos inventados; 0 arquitectura territorial; español. Gates re-ejecutados de verdad: 381/381 (44.30 s) · 0 errores/16 warnings · build 15.54 s · 41 visual (5.8 min). 0 producción tocada (AUDIT-LOG entrada 011).

10. **BLOQUE 5 EJECUTADO (2026-08-28)** — autorización: ChatGPT 🟢 "BLOQUE 4 APROBADO" + PROMPT MAESTRO de Sebastián (matriz final de decisión como sección final de PAGE-BLUEPRINTS.md — NO documento paralelo; jerarquía de fuentes; contradiction ledger; decisiones negativas; sobrecoreografía; reversibilidad; implementabilidad futura; protección de embudo; 3D solo decisión; D-01…D-07 solo si el plan los exige; gates reales; STOP absoluto). Pre-flight real (HEAD = origin/main = `aaf1543`, árbol limpio). Relectura del plan (§bloques: la matriz va como sección final del documento, líneas 138–141), PAGE-BLUEPRINTS completo, SCROLL-STORY-MATRIX, MOTION-MAP, docs centrales y código vivo del engine en SOLO LECTURA (`recipes/index.js` + test → 8 ids; `intensity.js`; `motionBudget.js` hero3/body2/supporting1/cta2/background0; `pageMotionContract.js` no tocado; sceneRegistry/Globe3D). Entregable: **secciones M.0–M.15** en `PAGE-BLUEPRINTS.md` — jerarquía de autoridad 9 niveles + protocolo de conflictos; taxonomía HARD CONSTRAINT/STRONG DEFAULT/CONDITIONAL/OPEN; matriz central de 19 filas con estado por fila (sin filas ABIERTAS); 16 decisiones negativas + triggers anti-sobre-diseño; presupuesto de atención por categoría; sobrecoreografía (2 ALTO `/` y `/parkour-academy` con degradaciones documentadas, 5 CONTROLADO, 12 BAJO); reversibilidad LOW/MEDIUM/HIGH; contradiction ledger C-1…C-8 (sin armonización silenciosa; C-7 registra la discrepancia de formato entre los dos prompts del auditor → gana el plan); prueba de implementabilidad en 5 categorías; ambigüedad sana vs peligrosa (ninguna peligrosa sin dueño; DP-5 = ABIERTA de Sebastián); matriz transversal (sin clustering problemático); test de memoria 14/14 + humanidad; contrato de handoff Fase 7 (MAY/MUST NOT/ESCALATE); diferenciación final 18/18 (10 parejas probadas). D-01…D-07 NO ejecutados — el plan no los asigna al cierre del Bloque 5 (M.14-K; no se fabrica cumplimiento). Autoauditoría con grep: solo las 8 recetas reales (horizontal-passage solo como decisión negativa), 0 inventadas, 0 APIs. Gates re-ejecutados de verdad: 381/381 (53.25 s) · 0 errores/16 warnings (pre-existentes; el diff no toca src/) · build 17.99 s (vendor-three 826.94 kB) · 41 visual (5.0 min). 0 producción tocada (AUDIT-LOG entrada 012).

11. **CIERRE TRANSVERSAL DE FASE 6 EJECUTADO (2026-08-28)** — autorización: auditor 🟢 "BLOQUE 5 APROBADO" + PROMPT MAESTRO de Sebastián (WORLD-3D-STRATEGY + tests D-01…D-07 + auditoría final; NO Fase 7) + PROMPT COMPLEMENTARIO de dirección espacial/3D. Pre-flight real (HEAD = origin/main = `9d00f12`, árbol limpio). **Inventario 3D real contra código:** lo vivo en `/about` es `GlobeTestimonials` (**mapa 2D interactivo**, 15 marcadores accesibles, textura earth-dark desde CDN externo unpkg) — NO WebGL; `Globe3D.jsx` (globo WebGL) **DORMANTE** (sin imports de producción); `SceneMount`+`signature`/`SignatureScene` **durmientes** (cableados en PageHero, 0 rutas declaran `scene=`, coste cero por lazy/code-splitting). Corrección declarada D-07a (medio de `/about` = mapa 2D; clasificación EXISTENTE conservada por jerarquía código>docs). Entregables: (1) `WORLD-3D-STRATEGY.md` (244 líneas A–U): 8 tipos de profundidad P1–P8 (con "immersive ≠ 3D"), prueba de necesidad 17 preguntas, **gate de admisión 8 puertas + veto humano**, clasificación 19 rutas + 9 mundos, performance en clases A–D, 16 anti-patterns, listas "NO NECESITA 3D"/"3D SOLO SI", matriz de admisión puntuada (**`/parkour-academy` = 27/40 → NO admisible hoy**, móvil 2 y espectáculo 2), registro final de consistencia W1–W6, handoff Fase 7 + plan de validación; (2) `src/test/fase6Contracts.test.js` (**D-01…D-07, 21 tests documentales**, 0 producción, 0 skip/only): D-01 cobertura, D-02 recetas reales, D-03 intensidades, D-04 9 mundos, D-05 clasificaciones 3D + embudo blindado, D-06 sin vocabulario paralelo, D-07 mecánica (parte humana documentada, no automatizada) + extra evidencia/idioma. **Decisión central: NO añadir ningún momento 3D nuevo en Fase 7; la web ya es espacial sin WebGL.** Autoauditoría: 21/21 tests pasan; grep 0 recetas inventadas. Gates al cierre: 402/402 (41.95 s) · 0/16 · build 19.67 s · 41 visual (5.0 min). 0 producción (AUDIT-LOG entrada 013).

12. **FASE 7A EJECUTADA (2026-08-30) — PERFORMANCE FORENSIC + 3D ADMISSION (diagnóstico A–J)** — autorización: PROMPT MAESTRO 1 + PROMPT MAESTRO 2 del auditor (PM1 alcance/ley; PM2 método/evidencia/etiquetas). Pre-flight Bloque A completo PASA (HEAD = origin/main = `f61c0a8`, árbol limpio, 500 archivos, fsck OK). **Hallazgo de primer orden 7A-01 (ESTADO B, MEDIDO):** `vendor-three` (807,56 kB min / **216,48 kB gzip**) se descarga en **TODAS** las rutas (18 rutas × 3 pases desktop/Pixel-7/reduced contra `vite preview` del build real; incluida la 404) porque el shell lo importa estáticamente: `main.jsx → ExperienceProvider.jsx:22 → Loader.jsx:26 (useProgress de @react-three/drei)` + `modulepreload` en dist/index.html. Fix NO ejecutado (scope del arquitecto; PM2 §8.5: reportar y parar). **CTR-01 resuelta en dirección INESPERADA:** PERFORMANCE-BASELINE §4 ("solo al visitar /about") mintió, pero la verdad es peor (fuga total); corrección DIFERIDA hasta el fix de 7A-01 para no escribir una tercera versión falsa. **Admisión candidato-01 `/parkour-academy` = A — REJECTED:** alternativa 2D IGUAL/MEJOR a coste ~0 (cinematic-stage sin implementar, foto real de salto, titulares ya ganan); G2/G4/G8 ROJAS; veto humano negativo; score re-evaluado 23/40 (vs 27/40). Sin prototipo — la fase NO añade 3D. **Entregables:** FASE7A-FORENSIC.md, 3D-ADMISSION-RECORD.md (con condición de reapertura triple), 3D-PERFORMANCE-BASELINE.md, guard fase7aSceneGovernance (7 tests, verificado por fallo inducido), three-network-audit.spec.js (contrato; aserción dura desactivada SOLO mientras 7A-01 vive), webvitals-lab.spec.js, scripts/measure-bundle.mjs, playwright.f7a.config.js (aditiva), artifacts/fase7a. Gates finales: 409/409 (73 ficheros) · lint 0/16 (15 errores propios corregidos en fase) · build 19,29 s · visual 64 passed (6,4 min). 0 producción (AUDIT-LOG entrada 014).
- Último commit de código `70606dd` (Fase 5); commits posteriores solo documentales (→ BLOQUES 1–5 → CIERRE TRANSVERSAL de Fase 6). Estado vivo: comprobar con `git rev-parse HEAD` (convención: no congelar HEAD vivo en docs).
- Fases 1–5 reales y aprobadas; ninguna página pública rediseñada en Fase 5.
- Contratos comerciales y SEO vivos en producción (precios, WhatsApp, canonical, sitemap 14, robots).
- **FASE 6 = WORLD BUILDING** (DF-006 APROBADA; plan APROBADO CON CORRECCIONES — D-008; V2.0 rige la ejecución; 0 código de producción, tests documentales permitidos).
- **BLOQUE 1 aprobado:** `BAYONA-WORLD-BIBLE.md` PARTE I (ADN VISUAL) — vinculante para Bloques 2–5 y Fases 7–8.
- **BLOQUE 2 aprobado:** `SPATIAL-LANGUAGE.md` (gramática espacial, 7 estados) — vinculante; relaciones con el engine conceptuales, no APIs. Condiciones DF-009…DF-012 registradas en DECISIONS.md.
- **BLOQUE 3 terminado:** `BAYONA-WORLD-BIBLE.md` PARTE II (mundos 00–08) — propuesta vinculante pendiente de auditoría; 9 mundos (0 fusiones, 2 refinamientos de definición), matrices de diferenciación/medios/resiliencia, intercambiabilidad superada (7 pares + 36 pairwise), 3D clasificado sin escenas.
- **BLOQUE 4 terminado y APROBADO por el auditor** (veredicto sobre `aaf1543`): `PAGE-BLUEPRINTS.md` — 18 planos de experiencia (B01–B18) con 15 apartados, auditoría de no-intercambiabilidad superada (TEST 1–4 + 5 parejas de riesgo), cobertura 16+1+1+1, mapa de relaciones y tabla §8.2; 3D solo clasificado.
- **BLOQUE 5 terminado y APROBADO por el auditor** (veredicto sobre `9d00f12`): MATRIZ FINAL DE DECISIÓN M.0–M.15 en PAGE-BLUEPRINTS.
- **CIERRE TRANSVERSAL DE FASE 6 TERMINADO (2026-08-28):** `WORLD-3D-STRATEGY.md` (A–U, 244 líneas) + tests D-01…D-07 (21). **Fase 6 documental COMPLETA** — 6 piezas: mundos, gramática, blueprints, matriz, estrategia 3D, contratos documentales.
- **Hallazgo clave:** la web ya es espacial sin WebGL (8 tipos de profundidad P1–P8; `/about` con mapa 2D de 15 personas). **Decisión de bloque: NO añadir ningún momento 3D nuevo en Fase 7**; `/parkour-academy` es candidato condicional (hoy 27/40, no admisible).
- Baseline re-ejecutada durante el CIERRE (2026-08-28): 402/402 (41.95 s) · 0/16 · build OK 19.67 s · 41 visual (5.0 min).

## ¿Qué está pendiente?
- **Veredicto del auditor sobre FASE 7A** (diagnóstico A–J: parkour REJECTED + hallazgo 7A-01).
- **PREGUNTA 7A-01 (cerrada, formulada en FASE7A-FORENSIC.md y 3D-ADMISSION-RECORD.md):** ¿Fase 7B mínima para el fix de la fuga (sustituir useProgress de drei en Loader.jsx → 216,48 kB gzip a 0 en todas las rutas) o deuda para Fase 12?
- **DP-5:** ELITE "acceso de por vida" (comercial/legal, solo Sebastián).
- FUTURE registrados: self-host earth-dark.jpg (W2), CLS ~0,18 imágenes sin dimensiones (F1 §6), cinematic-stage 2D en parkour (Fase 8 con blueprint).

## ¿Qué NO debo tocar?
- Precios, WhatsApp, catálogo, dominio, SEO global, sitemap, robots, rutas públicas, checkout, copy contractual.
- Resolución de DP-5 (es de Sebastián).
- Rediseñar páginas / componentes de producción / escenas 3D nuevas / dependencias durante Fase 6 (es documental); Fase 7–8 sin plan aprobado.
- Añadir motion libs o GSAP; skip/only en tests.

## ¿Qué debo revisar antes de actuar?
`git status` + `git rev-parse HEAD` vs origin/main → este HANDOFF → PROJECT-STATE → DECISIONS (DF-006 APROBADA, DP-5) → ROADMAP.

## ¿Cuál es el siguiente paso?
**STOP ABSOLUTO.** Esperar la auditoría de ChatGPT del CIERRE TRANSVERSAL de Fase 6 (informe entregado). Prohibido iniciar FASE 7, FASE 8 o cualquier implementación visual sin autorización explícita del auditor. Modelo vigente: FASE N → AUDITORÍA → FASE N+1, nunca auto-continuar. Anti-burocracia (DF-012): documentación necesaria sí, documentos que documentan documentos no.

## ¿Qué tests debo ejecutar?
`npm test` · `npm run lint` · `npm run build` · `npm run test:visual` — re-ejecutados de verdad durante el CIERRE TRANSVERSAL de Fase 6 (2026-08-28): **402/402 (41.95 s) · 0 errores/16 warnings · build OK 19.67 s (vendor-three 826.94 kB) · 41 visual passed (5.0 min)**. Regla V2.0 §23: nunca copiar estos números sin ejecución actual.

## ¿Dónde está la documentación?
- Central: `docs/` (este directorio).
- De fase: raíz del repo (convención consolidada, no mover).
- Auditoría forense completa: `docs/AUDITORIA-FORENSE-FASES-1-5.md`.
